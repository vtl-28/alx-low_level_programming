#include "main.h"
#include <stdlib.h>

/**
 * _abs - function that computes the absolute value of an integer.
 * @n: variable to check
 * Return: return int
 */

int _abs(int n)
{
	return (abs(n));
}
