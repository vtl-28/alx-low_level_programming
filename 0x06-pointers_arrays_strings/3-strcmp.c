#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings.
 * @s1: string one
 * @s2: string two
 * Return: return 0 if strings match
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
